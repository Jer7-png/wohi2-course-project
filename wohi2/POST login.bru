meta {
  name: POST login
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "test@test.com",
    "password": "password"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
